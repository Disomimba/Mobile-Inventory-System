name: inventory_plus
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  ultralytics_yolo:  # Official YOLO plugin for Flutter
  camera: ^0.11.0
  lucide_icons_flutter: ^3.1.12
  mobile_scanner: ^5.1.0
  shared_preferences: ^2.2.2
  supabase_flutter: ^2.0.0
  image_picker: ^1.0.7
  flutter_dotenv: ^6.0.1
  pdf: ^3.12.0
  printing: ^5.14.3
  fl_chart: ^1.2.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/.env
    - assets/models/best_float32.tflite
    - assets/models/metadata.yaml